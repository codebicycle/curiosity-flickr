<ul class="pagination">
  <% @out[:pagination][:pages].each do |page| %>
    <li>
    <% if page == @out[:pagination][:current] %>
      <%= page %>
    <% else %>
      <a href="#" class="pagination-link" data-param_page="<%= page %>"><%= page %></a>
    <% end %>
    </li>
  <% end %>
</ul>
